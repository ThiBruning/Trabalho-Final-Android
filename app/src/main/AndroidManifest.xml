<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ifpr.bruning.posts">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true">
        <activity android:name=".app.post.AuthorPostsActivity">
        </activity>
        <activity android:name=".app.comment.CommentFormActivity">
        </activity>
        <activity
                android:name=".app.NewsActivity"
                android:label="NewsActivity">
        </activity>
        <activity android:name=".app.post.PostInfoActivity">
        </activity>
        <activity android:name=".app.post.PostFormActivity">
        </activity>
        <activity android:name=".app.author.RegisterActivity">
        </activity>
        <activity android:name=".app.post.PostsActivity">
        </activity>
        <activity android:name=".app.author.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>